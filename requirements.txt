# ============================================================
# 校园政策 RAG 智能问答后端 — 依赖列表
# 安装命令: pip install -r requirements.txt
# 注意: sentence-transformers 会自动安装 torch（约 2GB），首次安装较慢
# ============================================================

# ==================== LlamaIndex 检索层 ====================
llama-index>=0.11.0
llama-index-vector-stores-qdrant>=0.3.0
llama-index-embeddings-huggingface>=0.3.0

# ==================== LangChain 对话编排层 ====================
langchain>=0.2.0
langchain-deepseek>=0.1.0
langchain-community>=0.2.0

# ==================== Embedding 向量模型（底层依赖 sentence-transformers + torch） ====================
sentence-transformers>=2.7.0

# ==================== 向量存储 Qdrant 本地文件模式 ====================
qdrant-client>=1.10.0

# ==================== Web 服务 ====================
fastapi>=0.110.0
uvicorn>=0.30.0
pydantic>=2.7.0

# ==================== 配置管理 ====================
python-dotenv>=1.0.0
